<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fredkontur.podcastplaylists">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SeeAllPlaylists" />
        <activity android:name=".SeePlaylist" />
        <activity android:name=".EnterComment" />
        <activity android:name=".CreateNewPodcast" />
        <activity android:name=".SeePodcast" />
        <activity android:name=".EditPlaylist" />
        <activity android:name=".EditPodcast" />
        <activity android:name=".RatePodcast" />
        <activity android:name=".DeleteResult" />
        <activity android:name=".SeeAllPodcasts" />
        <activity android:name=".CreateNewPlaylist" />
        <activity android:name=".AddPodcasts" />
        <activity android:name=".EnterEndorsement" />
        <activity android:name=".SeeMyPlaylists"></activity>
    </application>

</manifest>